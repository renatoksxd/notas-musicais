version: 2

mkdocs:
  configuration: mkdocs.yml

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

  jobs:
    post_create_environment:
      - pip install poetry
      - poetry config virtualenvs.create false

    post_install:
      - poetry install --only doc
      - which python
      - python --version
      - python -c "import sys; print(sys.executable)"
      - python -c "import material; print(material.__file__)"
